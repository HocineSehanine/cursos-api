spring:
  application:
    name: cursos-api

  datasource:
    url: jdbc:mysql://localhost:3306/db-cursos-api
    username: admin
    password: admin
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

